// desktop.js
// Stub for web/Vercel builds - only used in Neutralino desktop app
// The actual implementation runs only when window.NL_MODE is true

export async function initDesktop(_player) {
  // No-op in web/Vercel environment
  console.log('Desktop module: running in web mode, desktop features disabled');
}
