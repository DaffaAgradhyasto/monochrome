name: Restore js/settings.js

on:
  workflow_dispatch:

jobs:
  restore:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Restore js/settings.js from commit 235bfcc
        run: |
          git checkout 235bfcc74cafbcd25fcc151b132ee08db3d22a21 -- js/settings.js
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add js/settings.js
          git commit -m "fix: restore js/settings.js from working commit 235bfcc (fix Vercel build error)"
          git push
